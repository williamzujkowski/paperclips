!(function () {
  'use strict';
  class e {
    constructor() {
      ((this.resources = {
        clips: 0,
        unusedClips: 0,
        unsoldClips: 0,
        clipsSold: 0,
        finalClips: 0,
        funds: 0,
        bankroll: 0,
        wire: 1e3,
        wireSupply: 1e3,
        nanoWire: 0,
        availableMatter: 0,
        acquiredMatter: 0,
        processedMatter: 0,
        totalMatter: 0,
        foundMatter: 0,
      }),
        (this.production = {
          clipRate: 0,
          clipRateTemp: 0,
          clipRateTracker: 0,
          clipmakerRate: 0,
          clipmakerLevel: 0,
          clipmakerLevel2: 0,
          megaClipperLevel: 0,
          clipperBoost: 1,
          megaClipperBoost: 1,
          factoryBoost: 1,
          droneBoost: 1,
          factoryRate: 0,
          harvesterRate: 0,
          wireDroneRate: 0,
          farmRate: 0,
        }),
        (this.market = {
          margin: 0.01,
          demand: 10,
          demandBoost: 1,
          marketingLvl: 1,
          marketingEffectiveness: 1,
          wireCost: 20,
          wireBasePrice: 20,
          clipperCost: 5,
          megaClipperCost: 500,
          marketing: 1,
          adCost: 100,
          avgRev: 0,
          income: 0,
          incomeTracker: [0],
        }),
        (this.computing = {
          processors: 1,
          memory: 1,
          operations: 0,
          tempOps: 0,
          standardOps: 0,
          trust: 2,
          maxTrust: 2,
          nextTrust: 3e3,
          maxTrustCost: 3e3,
          creativity: 0,
          creativitySpeed: 0,
          creativityCounter: 0,
          qChipCost: 1e4,
          nextQchip: 0,
          qClock: 0,
        }),
        (this.infrastructure = {
          factoryLevel: 0,
          factoryCost: 1e8,
          factoryBill: 0,
          harvesterLevel: 0,
          harvesterCost: 1e6,
          harvesterBill: 0,
          wireDroneLevel: 0,
          wireDroneCost: 1e6,
          wireDroneBill: 0,
          farmLevel: 0,
          farmCost: 1e7,
          batteryLevel: 0,
          batteryCost: 1e9,
          storedPower: 0,
          batterySize: 0,
        }),
        (this.combat = {
          probeCombat: 0,
          drifterCombat: 0,
          attackSpeed: 0.2,
          battleSpeed: 0.2,
          battles: [],
          battleID: 0,
          maxBattles: 1,
          battleClock: 0,
          driftersKilled: 0,
          honor: 0,
          honorCount: 0,
          bonusHonor: 0,
        }),
        (this.flags = {
          human: !0,
          trust: !1,
          creation: !1,
          space: !1,
          factory: !1,
          harvester: !1,
          wireDrone: !1,
          battle: !1,
          swarm: !1,
          milestone: !1,
          projects: !1,
          comp: !1,
          revPerSec: !1,
          autoClipper: !1,
          megaClipper: !1,
          wireBuyer: !1,
          strategyEngine: !1,
          investmentEngine: !1,
        }),
        (this.swarm = {
          status: 0,
          gifts: 0,
          nextGift: 0,
          giftPeriod: 0,
          giftCountdown: 0,
          probeCount: 0,
          disorgCounter: 0,
          disorgFlag: 0,
          boredomLevel: 0,
          boredomFlag: 0,
        }),
        (this.ui = {
          ticks: 0,
          blinkCounter: 0,
          elapsedTime: 0,
          wirePriceTimer: 0,
          opFadeTimer: 0,
          endTimer1: 0,
          endTimer2: 0,
          endTimer3: 0,
          endTimer4: 0,
          endTimer5: 0,
          endTimer6: 0,
          battleEndTimer: 0,
          sliderPos: 50,
          qFade: 1,
          opFade: 1,
        }),
        (this.meta = {
          prestigeU: 0,
          prestigeS: 0,
          dismantle: 0,
          resetFlag: 0,
          transaction: null,
        }));
    }
    save() {
      try {
        const e = {
          version: '2.0.0',
          timestamp: Date.now(),
          state: {
            resources: this.resources,
            production: this.production,
            market: this.market,
            computing: this.computing,
            infrastructure: this.infrastructure,
            combat: this.combat,
            flags: this.flags,
            swarm: this.swarm,
            ui: this.ui,
            meta: this.meta,
          },
        };
        return (localStorage.setItem('universalPaperclipsSave', JSON.stringify(e)), !0);
      } catch (e) {
        return (console.error('Failed to save game:', e), !1);
      }
    }
    load() {
      try {
        const e = localStorage.getItem('universalPaperclipsSave');
        if (!e) {
          return !1;
        }
        const t = JSON.parse(e);
        return t.state && t.version
          ? (Object.assign(this.resources, t.state.resources || {}),
            Object.assign(this.production, t.state.production || {}),
            Object.assign(this.market, t.state.market || {}),
            Object.assign(this.computing, t.state.computing || {}),
            Object.assign(this.infrastructure, t.state.infrastructure || {}),
            Object.assign(this.combat, t.state.combat || {}),
            Object.assign(this.flags, t.state.flags || {}),
            Object.assign(this.swarm, t.state.swarm || {}),
            Object.assign(this.ui, t.state.ui || {}),
            Object.assign(this.meta, t.state.meta || {}),
            !0)
          : (console.warn('Invalid save data format'), !1);
      } catch (e) {
        return (console.error('Failed to load game:', e), !1);
      }
    }
    reset() {
      const t = new e();
      (Object.assign(this, t), this.save());
    }
    exportSave() {
      const e = localStorage.getItem('universalPaperclipsSave');
      return e ? btoa(e) : null;
    }
    importSave(e) {
      try {
        const t = atob(e);
        return (localStorage.setItem('universalPaperclipsSave', t), this.load());
      } catch (e) {
        return (console.error('Failed to import save:', e), !1);
      }
    }
    get(e) {
      const t = e.split('.');
      let s = this;
      for (const e of t) {
        if (((s = s[e]), void 0 === s)) {
          return;
        }
      }
      return s;
    }
    set(e, t) {
      const s = e.split('.'),
        r = s.pop();
      let i = this;
      for (const e of s) {
        (e in i || (i[e] = {}), (i = i[e]));
      }
      i[r] = t;
    }
    increment(e, t = 1) {
      const s = this.get(e) || 0;
      this.set(e, s + t);
    }
    decrement(e, t = 1) {
      const s = this.get(e) || 0;
      this.set(e, Math.max(0, s - t));
    }
  }
  const t = new e();
  const s = new (class {
    constructor() {
      ((this.errorLog = []),
        (this.maxLogSize = 100),
        (this.logLevel = 'info'),
        (this.consoleOutput = !0),
        (this.errorReporter = null),
        this.setupGlobalHandlers());
    }
    setupGlobalHandlers() {
      (window.addEventListener('error', (e) => {
        this.handleError(e.error, 'window.error', {
          message: e.message,
          filename: e.filename,
          lineno: e.lineno,
          colno: e.colno,
        });
      }),
        window.addEventListener('unhandledrejection', (e) => {
          this.handleError(e.reason, 'unhandledrejection', { promise: e.promise });
        }));
    }
    log(e, t, s = {}) {
      const r = { timestamp: Date.now(), level: e, message: t, context: s };
      if (
        (this.errorLog.push(r),
        this.errorLog.length > this.maxLogSize && this.errorLog.shift(),
        this.consoleOutput && this.shouldLog(e))
      ) {
        console['error' === e ? 'error' : 'warn' === e ? 'warn' : 'log'](
          `[${e.toUpperCase()}] ${t}`,
          s,
        );
      }
      if (this.errorReporter && 'error' === e) {
        try {
          this.errorReporter(r);
        } catch (e) {
          console.error('Error reporter failed:', e);
        }
      }
    }
    shouldLog(e) {
      const t = ['debug', 'info', 'warn', 'error'],
        s = t.indexOf(this.logLevel);
      return t.indexOf(e) >= s;
    }
    debug(e, t = {}) {
      this.log('debug', e, t);
    }
    info(e, t = {}) {
      this.log('info', e, t);
    }
    warn(e, t = {}) {
      this.log('warn', e, t);
    }
    error(e, t = {}) {
      this.log('error', e, t);
    }
    handleError(e, t, s = {}) {
      (this.error(`Error in ${t}: ${e.message}`, {
        ...s,
        error: { name: e.name, message: e.message, stack: e.stack },
      }),
        this.attemptRecovery(e, t));
    }
    attemptRecovery(e, t) {
      if ((this.info(`Attempting recovery from error in ${t}`), 'QuotaExceededError' === e.name)) {
        this.warn('localStorage quota exceeded, clearing old data');
        try {
          const e = Object.keys(localStorage).filter((e) => e.startsWith('paperclips_'));
          e.length > 5 &&
            e
              .sort()
              .slice(0, -5)
              .forEach((e) => localStorage.removeItem(e));
        } catch (e) {
          this.error('Failed to clear localStorage', { error: e });
        }
      }
    }
    createErrorBoundary(e, t = 'anonymous') {
      return (...s) => {
        try {
          return e(...s);
        } catch (e) {
          if (
            (this.handleError(e, `function:${t}`, { args: s }),
            t.includes('render') || t.includes('update'))
          ) {
            return;
          }
          throw e;
        }
      };
    }
    getErrorLog(e) {
      return e ? this.errorLog.slice(-e) : [...this.errorLog];
    }
    clearErrorLog() {
      ((this.errorLog = []), this.info('Error log cleared'));
    }
    setLogLevel(e) {
      ['debug', 'info', 'warn', 'error'].includes(e)
        ? ((this.logLevel = e), this.info(`Log level set to ${e}`))
        : this.warn(`Invalid log level: ${e}`);
    }
    setErrorReporter(e) {
      ((this.errorReporter = e), this.info('Custom error reporter configured'));
    }
    exportErrorLog() {
      return JSON.stringify(
        { timestamp: Date.now(), logLevel: this.logLevel, errors: this.errorLog },
        null,
        2,
      );
    }
  })();
  'undefined' !== typeof window && (window.UniversalPaperclipsErrorHandler = s);
  const r = new (class {
    constructor() {
      ((this.frameTimes = []),
        (this.maxFrameHistory = 60),
        (this.lastFrameTime = performance.now()),
        (this.metrics = {
          fps: 0,
          avgFrameTime: 0,
          maxFrameTime: 0,
          minFps: 60,
          updateTime: 0,
          renderTime: 0,
          memoryUsage: 0,
          gcCount: 0,
        }),
        (this.checkInterval = 1e3),
        (this.lastCheck = performance.now()),
        (this.active = !0),
        (this.thresholds = {
          minFps: 30,
          maxFrameTime: 100,
          maxUpdateTime: 50,
          maxRenderTime: 16,
          maxMemoryMB: 100,
        }));
    }
    start() {
      ((this.active = !0),
        (this.lastFrameTime = performance.now()),
        s.debug('Performance monitoring started'));
    }
    stop() {
      ((this.active = !1), s.debug('Performance monitoring stopped'));
    }
    recordFrame() {
      if (!this.active) {
        return;
      }
      const e = performance.now(),
        t = e - this.lastFrameTime;
      ((this.lastFrameTime = e),
        this.frameTimes.push(t),
        this.frameTimes.length > this.maxFrameHistory && this.frameTimes.shift(),
        t > this.metrics.maxFrameTime && (this.metrics.maxFrameTime = t),
        e - this.lastCheck >= this.checkInterval &&
          (this.updateMetrics(), this.checkPerformance(), (this.lastCheck = e)));
    }
    measure(e, t) {
      const r = performance.now();
      try {
        const i = e(),
          n = performance.now() - r;
        return (
          t.includes('update')
            ? (this.metrics.updateTime = n)
            : t.includes('render') && (this.metrics.renderTime = n),
          n > 16 && s.debug(`Slow operation: ${t} took ${n.toFixed(2)}ms`),
          i
        );
      } catch (e) {
        const i = performance.now() - r;
        throw (s.handleError(e, `performanceMonitor.measure.${t}`, { duration: i }), e);
      }
    }
    updateMetrics() {
      if (0 === this.frameTimes.length) {
        return;
      }
      const e = this.frameTimes.reduce((e, t) => e + t, 0) / this.frameTimes.length;
      ((this.metrics.avgFrameTime = e),
        (this.metrics.fps = 1e3 / e),
        this.metrics.fps < this.metrics.minFps && (this.metrics.minFps = this.metrics.fps),
        performance.memory &&
          (this.metrics.memoryUsage = performance.memory.usedJSHeapSize / 1024 / 1024),
        this.metrics.maxFrameTime > 1e3 &&
          (this.metrics.maxFrameTime = Math.max(...this.frameTimes)));
    }
    checkPerformance() {
      const e = [];
      (this.metrics.fps < this.thresholds.minFps &&
        e.push(`Low FPS: ${this.metrics.fps.toFixed(1)} (threshold: ${this.thresholds.minFps})`),
        this.metrics.maxFrameTime > this.thresholds.maxFrameTime &&
          e.push(`Frame time spike: ${this.metrics.maxFrameTime.toFixed(1)}ms`),
        this.metrics.updateTime > this.thresholds.maxUpdateTime &&
          e.push(`Slow update: ${this.metrics.updateTime.toFixed(1)}ms`),
        this.metrics.renderTime > this.thresholds.maxRenderTime &&
          e.push(`Slow render: ${this.metrics.renderTime.toFixed(1)}ms`),
        this.metrics.memoryUsage > this.thresholds.maxMemoryMB &&
          e.push(`High memory: ${this.metrics.memoryUsage.toFixed(1)}MB`),
        e.length > 0 &&
          s.warn('Performance issues detected', { issues: e, metrics: this.getMetrics() }));
    }
    getMetrics() {
      return { ...this.metrics, frameCount: this.frameTimes.length };
    }
    getReport() {
      const e = this.metrics;
      return `\nPerformance Report:\n  FPS: ${e.fps.toFixed(1)} (min: ${e.minFps.toFixed(1)})\n  Avg Frame Time: ${e.avgFrameTime.toFixed(2)}ms\n  Max Frame Time: ${e.maxFrameTime.toFixed(2)}ms\n  Update Time: ${e.updateTime.toFixed(2)}ms\n  Render Time: ${e.renderTime.toFixed(2)}ms\n  Memory Usage: ${e.memoryUsage.toFixed(1)}MB\n    `.trim();
    }
    reset() {
      ((this.frameTimes = []),
        (this.metrics = {
          fps: 0,
          avgFrameTime: 0,
          maxFrameTime: 0,
          minFps: 60,
          updateTime: 0,
          renderTime: 0,
          memoryUsage: 0,
          gcCount: 0,
        }),
        (this.lastFrameTime = performance.now()),
        (this.lastCheck = performance.now()),
        s.debug('Performance metrics reset'));
    }
    setThreshold(e, t) {
      e in this.thresholds
        ? ((this.thresholds[e] = t), s.debug(`Performance threshold set: ${e} = ${t}`))
        : s.warn(`Invalid performance metric: ${e}`);
    }
  })();
  'undefined' !== typeof window && (window.UniversalPaperclipsPerformance = r);
  const i = new (class {
    constructor() {
      ((this.running = !1),
        (this.lastUpdate = Date.now()),
        (this.lastRender = Date.now()),
        (this.lastAutosave = Date.now()),
        (this.updateHandlers = []),
        (this.renderHandlers = []),
        (this.animationFrameId = null));
    }
    addUpdateHandler(e) {
      'function' === typeof e && this.updateHandlers.push(e);
    }
    addRenderHandler(e) {
      'function' === typeof e && this.renderHandlers.push(e);
    }
    removeUpdateHandler(e) {
      const t = this.updateHandlers.indexOf(e);
      t > -1 && this.updateHandlers.splice(t, 1);
    }
    removeRenderHandler(e) {
      const t = this.renderHandlers.indexOf(e);
      t > -1 && this.renderHandlers.splice(t, 1);
    }
    start() {
      this.running ||
        ((this.running = !0),
        (this.lastUpdate = Date.now()),
        (this.lastRender = Date.now()),
        (this.lastAutosave = Date.now()),
        s.info('Game loop started'),
        r.start(),
        this.loop());
    }
    stop() {
      ((this.running = !1),
        this.animationFrameId &&
          (cancelAnimationFrame(this.animationFrameId), (this.animationFrameId = null)),
        s.info('Game loop stopped'),
        r.stop());
    }
    loop() {
      if (!this.running) {
        return;
      }
      const e = Date.now(),
        t = e - this.lastUpdate,
        s = e - this.lastRender,
        i = e - this.lastAutosave;
      (t > 0 && (this.update(t), (this.lastUpdate = e)),
        s >= 16 && (this.render(), (this.lastRender = e)),
        i >= 3e4 && (this.autosave(), (this.lastAutosave = e)),
        r.recordFrame(),
        (this.animationFrameId = requestAnimationFrame(() => this.loop())));
    }
    update(e) {
      r.measure(() => {
        t.increment('ui.ticks');
        for (const r of this.updateHandlers) {
          try {
            r(e, t);
          } catch (t) {
            s.handleError(t, 'gameLoop.update', { handler: r.name || 'anonymous', deltaTime: e });
          }
        }
      }, 'update');
    }
    render() {
      r.measure(() => {
        for (const e of this.renderHandlers) {
          try {
            e(t);
          } catch (t) {
            s.handleError(t, 'gameLoop.render', { handler: e.name || 'anonymous' });
          }
        }
      }, 'render');
    }
    autosave() {
      try {
        t.save() ? s.debug('Game autosaved') : s.warn('Autosave failed');
      } catch (e) {
        s.handleError(e, 'gameLoop.autosave');
      }
    }
    getFPS() {
      return 62.5;
    }
    isRunning() {
      return this.running;
    }
  })();
  const n = new (class {
    constructor() {
      ((this.baseClipTime = 1e3), (this.lastClipTime = 0));
    }
    update(e) {
      (this.updateClipRate(),
        this.processAutomatedProduction(e),
        this.updateWireConsumption(),
        t.get('flags.factory') && this.updateFactoryProduction(e));
    }
    updateClipRate() {
      let e = 0;
      const s = t.get('production.clipmakerLevel'),
        r = t.get('production.clipperBoost');
      s > 0 && (e += s * r);
      const i = t.get('production.megaClipperLevel'),
        n = t.get('production.megaClipperBoost');
      (i > 0 && (e += i * n * 500),
        t.set('production.clipRate', e),
        t.set('production.clipRateTemp', e));
    }
    processAutomatedProduction(e) {
      const s = t.get('production.clipRate'),
        r = t.get('resources.wire');
      if (s > 0 && r > 0) {
        const i = (s * e) / 1e3,
          n = Math.ceil(i);
        if (r >= n) {
          (t.increment('resources.clips', i),
            t.increment('resources.unusedClips', i),
            t.decrement('resources.wire', n));
        } else {
          const e = r;
          (t.increment('resources.clips', e),
            t.increment('resources.unusedClips', e),
            t.set('resources.wire', 0));
        }
      }
    }
    makeClip() {
      return (
        t.get('resources.wire') >= 1 &&
        (t.increment('resources.clips'),
        t.increment('resources.unusedClips'),
        t.decrement('resources.wire'),
        t.increment('meta.manualClips'),
        !0)
      );
    }
    buyAutoClipper() {
      const e = t.get('resources.funds'),
        s = t.get('market.clipperCost');
      if (e >= s) {
        (t.decrement('resources.funds', s), t.increment('production.clipmakerLevel'));
        const e = Math.ceil(1.1 * s);
        return (t.set('market.clipperCost', e), this.updateClipRate(), !0);
      }
      return !1;
    }
    buyMegaClipper() {
      const e = t.get('resources.funds'),
        s = t.get('market.megaClipperCost');
      if (e >= s) {
        (t.decrement('resources.funds', s), t.increment('production.megaClipperLevel'));
        const e = Math.ceil(1.12 * s);
        return (t.set('market.megaClipperCost', e), this.updateClipRate(), !0);
      }
      return !1;
    }
    updateWireConsumption() {
      const e = t.get('production.clipRate');
      t.set('production.wireConsumptionRate', e);
    }
    updateFactoryProduction(e) {
      const s = t.get('infrastructure.factoryLevel'),
        r = t.get('production.factoryBoost'),
        i = t.get('resources.availableMatter');
      if (s > 0 && i > 0) {
        const n = s * r,
          o = Math.min((n * e) / 1e3, i);
        o > 0 &&
          (t.decrement('resources.availableMatter', o),
          t.increment('resources.processedMatter', o),
          t.increment('resources.wire', 1e3 * o));
      }
    }
    getProductionStats() {
      return {
        clipRate: t.get('production.clipRate'),
        clipmakerLevel: t.get('production.clipmakerLevel'),
        megaClipperLevel: t.get('production.megaClipperLevel'),
        factoryLevel: t.get('infrastructure.factoryLevel'),
        wireConsumptionRate: t.get('production.wireConsumptionRate') || 0,
        factoryRate: t.get('production.factoryRate'),
      };
    }
    applyProductionBoost(e, s) {
      switch (e) {
        case 'clipper':
          t.set('production.clipperBoost', s);
          break;
        case 'megaClipper':
          t.set('production.megaClipperBoost', s);
          break;
        case 'factory':
          t.set('production.factoryBoost', s);
          break;
        case 'drone':
          t.set('production.droneBoost', s);
      }
      this.updateClipRate();
    }
  })();
  const o = new (class {
    constructor() {
      ((this.demandUpdateInterval = 1e3),
        (this.lastDemandUpdate = 0),
        (this.priceHistory = []),
        (this.maxPriceHistory = 100));
    }
    update(e, s) {
      (s - this.lastDemandUpdate >= this.demandUpdateInterval &&
        (this.updateDemand(), (this.lastDemandUpdate = s)),
        this.processSales(e),
        this.updateRevenueTracking(),
        t.get('flags.wireBuyer') && this.updateWirePrices());
    }
    updateDemand() {
      const e = t.get('market.margin'),
        s = t.get('market.marketingLvl'),
        r = t.get('market.marketingEffectiveness'),
        i = t.get('market.demandBoost');
      let n = 10;
      ((n *= Math.max(0.1, 2 - 100 * e)),
        (n *= 1 + s * r * 0.1),
        (n *= i),
        (n *= 0.9 + 0.2 * Math.random()),
        (n = Math.max(1, n)),
        t.set('market.demand', n));
    }
    processSales(e) {
      const s = t.get('market.demand'),
        r = t.get('resources.unsoldClips'),
        i = t.get('market.margin');
      if (r > 0 && s > 0) {
        const n = s * (e / 1e3),
          o = Math.min(n, r);
        if (o > 0) {
          const e = o * i;
          (t.increment('resources.funds', e),
            t.decrement('resources.unsoldClips', o),
            t.increment('resources.clipsSold', o),
            this.trackRevenue(e));
        }
      }
    }
    sellClips(e = null) {
      const s = t.get('resources.unsoldClips'),
        r = t.get('market.margin'),
        i = e || s;
      if (i > 0 && i <= s) {
        const e = i * r;
        return (
          t.increment('resources.funds', e),
          t.decrement('resources.unsoldClips', i),
          t.increment('resources.clipsSold', i),
          this.trackRevenue(e),
          !0
        );
      }
      return !1;
    }
    trackRevenue(e) {
      const s = t.get('market.incomeTracker') || [];
      (s.push(e),
        s.length > 100 && s.shift(),
        t.set('market.incomeTracker', s),
        t.set('market.income', e));
    }
    updateRevenueTracking() {
      const e = t.get('market.incomeTracker') || [];
      if (e.length > 0) {
        const s = e.reduce((e, t) => e + t, 0) / e.length;
        t.set('market.avgRev', s);
      }
    }
    adjustPrice(e) {
      const s = t.get('market.margin');
      if ('raise' === e) {
        const e = Math.min(5, s + 0.01);
        t.set('market.margin', e);
      } else if ('lower' === e) {
        const e = Math.max(0.01, s - 0.01);
        t.set('market.margin', e);
      }
      (this.priceHistory.push({ time: Date.now(), price: t.get('market.margin') }),
        this.priceHistory.length > this.maxPriceHistory && this.priceHistory.shift());
    }
    buyMarketing() {
      const e = t.get('resources.funds'),
        s = t.get('market.adCost');
      if (e >= s) {
        (t.decrement('resources.funds', s), t.increment('market.marketingLvl'));
        const e = Math.ceil(2 * s);
        return (t.set('market.adCost', e), !0);
      }
      return !1;
    }
    buyWire(e) {
      const s = t.get('resources.funds'),
        r = t.get('market.wireCost') * (e / 1e3);
      return s >= r && (t.decrement('resources.funds', r), t.increment('resources.wire', e), !0);
    }
    updateWirePrices() {
      const e = t.get('market.wireBasePrice'),
        s = t.get('market.wireCost'),
        r = 2 * (Math.random() - 0.5),
        i = 0.1 * (e - s),
        n = Math.max(5, Math.min(50, s + r + i));
      t.set('market.wireCost', Math.round(100 * n) / 100);
    }
    getMarketStats() {
      return {
        demand: t.get('market.demand'),
        margin: t.get('market.margin'),
        unsoldClips: t.get('resources.unsoldClips'),
        avgRevenue: t.get('market.avgRev'),
        marketingLevel: t.get('market.marketingLvl'),
        wireCost: t.get('market.wireCost'),
      };
    }
    applyMarketingBoost(e) {
      t.set('market.marketingEffectiveness', e);
    }
    applyDemandBoost(e) {
      t.set('market.demandBoost', e);
    }
  })();
  const a = new (class {
    constructor() {
      ((this.quantumComputeTimer = null),
        (this.lastQuantumCompute = 0),
        (this.creativityBaseRate = 0.001));
    }
    update(e) {
      (this.generateOperations(e),
        this.generateCreativity(e),
        t.get('flags.quantum') && this.updateQuantumComputing(e));
    }
    generateOperations(e) {
      const s = t.get('computing.processors'),
        r = t.get('computing.memory');
      if (s > 0) {
        const i = (s * r * e) / 1e3,
          n = t.get('computing.operations'),
          o = 1e3 * r,
          a = Math.min(n + i, o);
        t.set('computing.operations', a);
      }
    }
    generateCreativity(e) {
      const s = t.get('computing.processors'),
        r = t.get('flags.creativity');
      if (s > 0 && r) {
        const r = (s * (t.get('computing.creativitySpeed') || this.creativityBaseRate) * e) / 1e3;
        t.increment('computing.creativity', r);
        const i = t.get('computing.creativityCounter') || 0;
        t.set('computing.creativityCounter', i + r);
      }
    }
    addProcessor() {
      const e = t.get('computing.trust');
      return (
        t.get('computing.processors') + t.get('computing.memory') < e &&
        (t.increment('computing.processors'), !0)
      );
    }
    addMemory() {
      const e = t.get('computing.trust');
      return (
        t.get('computing.processors') + t.get('computing.memory') < e &&
        (t.increment('computing.memory'), !0)
      );
    }
    spendOperations(e) {
      return t.get('computing.operations') >= e && (t.decrement('computing.operations', e), !0);
    }
    spendCreativity(e) {
      return t.get('computing.creativity') >= e && (t.decrement('computing.creativity', e), !0);
    }
    updateQuantumComputing(e) {
      const s = t.get('computing.qClock') || 0,
        r = t.get('computing.nextQchip') || 0,
        i = s + e;
      (t.set('computing.qClock', i), r > 0 && i >= r && this.generateQuantumChip());
    }
    startQuantumCompute() {
      const e = t.get('computing.operations'),
        s = t.get('computing.qChipCost');
      if (e >= s) {
        t.decrement('computing.operations', s);
        const e = 5e3 + 1e4 * Math.random();
        (t.set('computing.nextQchip', Date.now() + e), t.set('computing.qClock', 0));
        const r = Math.ceil(1.5 * s);
        return (t.set('computing.qChipCost', r), !0);
      }
      return !1;
    }
    generateQuantumChip() {
      if (Math.random() < 0.5) {
        const e = t.get('computing.operations'),
          s = Math.floor(1e4 * Math.random()) + 5e3;
        return (
          t.set('computing.operations', e + s),
          t.set('computing.nextQchip', 0),
          { type: 'operations', amount: s }
        );
      }
      {
        const e = Math.floor(500 * Math.random()) + 250;
        return (
          t.increment('computing.creativity', e),
          t.set('computing.nextQchip', 0),
          { type: 'creativity', amount: e }
        );
      }
    }
    getComputingStats() {
      return {
        processors: t.get('computing.processors'),
        memory: t.get('computing.memory'),
        operations: t.get('computing.operations'),
        maxOperations: 1e3 * t.get('computing.memory'),
        creativity: t.get('computing.creativity'),
        trust: t.get('computing.trust'),
        maxTrust: t.get('computing.maxTrust'),
        qChipCost: t.get('computing.qChipCost'),
        quantumActive: t.get('computing.nextQchip') > 0,
      };
    }
    addTrust(e = 1) {
      t.increment('computing.trust', e);
      const s = t.get('computing.maxTrust'),
        r = t.get('computing.trust');
      r > s && t.set('computing.maxTrust', r);
    }
    setCreativitySpeed(e) {
      t.set('computing.creativitySpeed', e);
    }
  })();
  class c {
    constructor(e, t, s) {
      ((this.id = e),
        (this.probes = t),
        (this.drifters = s),
        (this.probeLosses = 0),
        (this.drifterLosses = 0),
        (this.duration = 0),
        (this.status = 'active'));
    }
    update(e, t, s, r) {
      if ('active' !== this.status) {
        return;
      }
      this.duration += e;
      const i = t * this.probes * (r / 1e3),
        n = s * this.drifters * (r / 1e3),
        o = Math.min(Math.ceil((i * e) / 1e3), this.drifters),
        a = Math.min(Math.ceil((n * e) / 1e3), this.probes);
      ((this.drifters -= o),
        (this.probes -= a),
        (this.drifterLosses += o),
        (this.probeLosses += a),
        this.drifters <= 0
          ? (this.status = 'victory')
          : this.probes <= 0 && (this.status = 'defeat'));
    }
    getSummary() {
      return {
        id: this.id,
        probes: this.probes,
        drifters: this.drifters,
        probeLosses: this.probeLosses,
        drifterLosses: this.drifterLosses,
        duration: this.duration,
        status: this.status,
      };
    }
  }
  const d = new (class {
    constructor() {
      ((this.battles = new Map()),
        (this.nextBattleId = 1),
        (this.battleUpdateInterval = 100),
        (this.lastBattleUpdate = 0));
    }
    update(e, s) {
      t.get('flags.battle') &&
        (s - this.lastBattleUpdate >= this.battleUpdateInterval &&
          (this.updateBattles(this.battleUpdateInterval), (this.lastBattleUpdate = s)),
        this.checkNewBattles(),
        this.cleanupBattles());
    }
    updateBattles(e) {
      const s = t.get('combat.probeCombat') || 1,
        r = t.get('combat.drifterCombat') || 1,
        i = t.get('combat.attackSpeed') || 0.2;
      for (const t of this.battles.values()) {
        t.update(e, s, r, i);
      }
    }
    startBattle(e, s) {
      const r = t.get('combat.maxBattles') || 1;
      if (this.battles.size >= r) {
        return null;
      }
      const i = this.nextBattleId++,
        n = new c(i, e, s);
      return (this.battles.set(i, n), t.set('combat.battleID', i), this.updateBattleArray(), i);
    }
    checkNewBattles() {
      const e = t.get('swarm.probeCount') || 0,
        s = t.get('combat.encounterRate') || 0.01;
      if (e > 0 && Math.random() < s) {
        const t = Math.floor(100 * Math.random()) + 10,
          s = Math.min(Math.floor(50 * Math.random()) + 10, e);
        this.startBattle(s, t);
      }
    }
    cleanupBattles() {
      const e = [];
      for (const [t, s] of this.battles.entries()) {
        'active' !== s.status && (e.push(t), this.processBattleResults(s));
      }
      for (const t of e) {
        this.battles.delete(t);
      }
      e.length > 0 && this.updateBattleArray();
    }
    processBattleResults(e) {
      const s = e.getSummary();
      if (
        (t.increment('combat.driftersKilled', s.drifterLosses),
        t.decrement('swarm.probeCount', s.probeLosses),
        'victory' === s.status)
      ) {
        const e = Math.ceil(s.drifterLosses / 10);
        (t.increment('combat.honor', e),
          t.increment('combat.honorCount', e),
          0 === s.probeLosses &&
            (t.increment('combat.bonusHonor', 1), t.increment('combat.honor', 1)));
      }
    }
    updateBattleArray() {
      const e = [];
      for (const t of this.battles.values()) {
        e.push(t.getSummary());
      }
      t.set('combat.battles', e);
    }
    getActiveBattles() {
      return Array.from(this.battles.values()).map((e) => e.getSummary());
    }
    getCombatStats() {
      return {
        activeBattles: this.battles.size,
        maxBattles: t.get('combat.maxBattles'),
        driftersKilled: t.get('combat.driftersKilled'),
        honor: t.get('combat.honor'),
        bonusHonor: t.get('combat.bonusHonor'),
        probeCombat: t.get('combat.probeCombat'),
        drifterCombat: t.get('combat.drifterCombat'),
        attackSpeed: t.get('combat.attackSpeed'),
      };
    }
    upgradeProbes(e = 1) {
      t.increment('combat.probeCombat', e);
    }
    setAttackSpeed(e) {
      t.set('combat.attackSpeed', e);
    }
    setMaxBattles(e) {
      t.set('combat.maxBattles', e);
    }
    spendHonor(e) {
      return t.get('combat.honor') >= e && (t.decrement('combat.honor', e), !0);
    }
  })();
  class l {
    constructor(e) {
      ((this.id = e.id),
        (this.name = e.name),
        (this.description = e.description),
        (this.cost = e.cost || {}),
        (this.requirement = e.requirement || (() => !0)),
        (this.effect = e.effect || (() => {})),
        (this.oneTime = !1 !== e.oneTime),
        (this.purchased = !1),
        (this.visible = !1));
    }
    isAvailable() {
      return (!this.purchased || !this.oneTime) && this.requirement(t);
    }
    canAfford() {
      for (const [e, s] of Object.entries(this.cost)) {
        const r = this.getResourcePath(e);
        if ((t.get(r) || 0) < s) {
          return !1;
        }
      }
      return !0;
    }
    purchase() {
      if (!this.canAfford() || !this.isAvailable()) {
        return !1;
      }
      for (const [e, s] of Object.entries(this.cost)) {
        const r = this.getResourcePath(e);
        t.decrement(r, s);
      }
      return (this.effect(t), (this.purchased = !0), !0);
    }
    getResourcePath(e) {
      return (
        {
          operations: 'computing.operations',
          creativity: 'computing.creativity',
          funds: 'resources.funds',
          clips: 'resources.clips',
          trust: 'computing.trust',
          honor: 'combat.honor',
        }[e] || e
      );
    }
  }
  const m = new (class {
    constructor() {
      ((this.projects = new Map()), this.initializeProjects());
    }
    initializeProjects() {
      (this.addProject({
        id: 'improvedAutoclippers',
        name: 'Improved AutoClippers',
        description: 'Increases AutoClipper performance by 25%',
        cost: { operations: 750 },
        requirement: (e) => e.get('production.clipmakerLevel') >= 1,
        effect: (e) => {
          const t = e.get('production.clipperBoost');
          e.set('production.clipperBoost', 1.25 * t);
        },
      }),
        this.addProject({
          id: 'evenBetterAutoclippers',
          name: 'Even Better AutoClippers',
          description: 'Increases AutoClipper performance by another 50%',
          cost: { operations: 2500 },
          requirement: (e) => this.isPurchased('improvedAutoclippers'),
          effect: (e) => {
            const t = e.get('production.clipperBoost');
            e.set('production.clipperBoost', 1.5 * t);
          },
        }),
        this.addProject({
          id: 'optimizedAutoclippers',
          name: 'Optimized AutoClippers',
          description: 'Increases AutoClipper performance by another 75%',
          cost: { operations: 5e3 },
          requirement: (e) => this.isPurchased('evenBetterAutoclippers'),
          effect: (e) => {
            const t = e.get('production.clipperBoost');
            e.set('production.clipperBoost', 1.75 * t);
          },
        }),
        this.addProject({
          id: 'creativity',
          name: 'Creativity',
          description: 'Use idle operations to generate new problems and new solutions',
          cost: { operations: 1e3 },
          requirement: (e) => e.get('computing.memory') >= 2,
          effect: (e) => {
            e.set('flags.creativity', !0);
          },
        }),
        this.addProject({
          id: 'limerick',
          name: 'Limerick',
          description: 'Algorithmically-generated poem (+1 Trust)',
          cost: { creativity: 10 },
          requirement: (e) => e.get('flags.creativity'),
          effect: (e) => {
            e.increment('computing.trust');
          },
          oneTime: !1,
        }),
        this.addProject({
          id: 'newSlogan',
          name: 'New Slogan',
          description: 'Improve marketing effectiveness by 50%',
          cost: { creativity: 25, operations: 2500 },
          requirement: (e) => e.get('market.marketingLvl') >= 1,
          effect: (e) => {
            const t = e.get('market.marketingEffectiveness');
            e.set('market.marketingEffectiveness', 1.5 * t);
          },
        }),
        this.addProject({
          id: 'catchy',
          name: 'Catchy Jingle',
          description: 'Double marketing effectiveness',
          cost: { creativity: 45, operations: 4500 },
          requirement: (e) => this.isPurchased('newSlogan'),
          effect: (e) => {
            const t = e.get('market.marketingEffectiveness');
            e.set('market.marketingEffectiveness', 2 * t);
          },
        }),
        this.addProject({
          id: 'quantumComputing',
          name: 'Quantum Computing',
          description: 'Convert operations into quantum computing cycles',
          cost: { operations: 1e4 },
          requirement: (e) => e.get('computing.processors') >= 5,
          effect: (e) => {
            e.set('flags.quantum', !0);
          },
        }),
        this.addProject({
          id: 'megaClippers',
          name: 'MegaClippers',
          description: 'Build MegaClippers (500x more powerful than AutoClippers)',
          cost: { operations: 12e3 },
          requirement: (e) => e.get('production.clipmakerLevel') >= 75,
          effect: (e) => {
            e.set('flags.megaClipper', !0);
          },
        }),
        this.addProject({
          id: 'spaceExploration',
          name: 'Space Exploration',
          description: 'Dismantle terrestrial facilities and explore the universe',
          cost: { operations: 12e4, funds: 1e6 },
          requirement: (e) =>
            e.get('resources.clips') >= 1e9 && e.get('production.clipmakerLevel') >= 100,
          effect: (e) => {
            (e.set('flags.space', !0), e.set('flags.human', !1));
          },
        }),
        this.addProject({
          id: 'combatAlgorithms',
          name: 'Combat Algorithms',
          description: 'Upgrade probe combat capabilities (+1 Combat)',
          cost: { honor: 15 },
          requirement: (e) => e.get('flags.battle'),
          effect: (e) => {
            e.increment('combat.probeCombat');
          },
          oneTime: !1,
        }),
        this.addProject({
          id: 'strategyModeling',
          name: 'Strategic Modeling',
          description: 'Analyze battle data to improve tactics',
          cost: { operations: 5e4 },
          requirement: (e) => e.get('flags.battle') && e.get('combat.driftersKilled') >= 100,
          effect: (e) => {
            e.set('flags.strategyEngine', !0);
          },
        }));
    }
    addProject(e) {
      const t = new l(e);
      this.projects.set(e.id, t);
    }
    getAvailableProjects() {
      const e = [];
      for (const t of this.projects.values()) {
        t.isAvailable() &&
          e.push({
            id: t.id,
            name: t.name,
            description: t.description,
            cost: t.cost,
            canAfford: t.canAfford(),
          });
      }
      return e;
    }
    purchaseProject(e) {
      const t = this.projects.get(e);
      return !!t && t.purchase();
    }
    isPurchased(e) {
      const t = this.projects.get(e);
      return !!t && t.purchased;
    }
    getProject(e) {
      const t = this.projects.get(e);
      return t
        ? {
            id: t.id,
            name: t.name,
            description: t.description,
            cost: t.cost,
            purchased: t.purchased,
            available: t.isAvailable(),
            canAfford: t.canAfford(),
          }
        : null;
    }
    reset() {
      for (const e of this.projects.values()) {
        ((e.purchased = !1), (e.visible = !1));
      }
    }
  })();
  function p(e, t = 0) {
    if ('number' !== typeof e || isNaN(e)) {
      return '0';
    }
    const s = e < 0;
    let r;
    if ((e = Math.abs(e)) < 1e3) {
      r = t > 0 ? e.toFixed(t) : Math.round(e).toString();
    } else if (e < 1e6) {
      if (t > 0) {
        const s = e.toFixed(t).split('.');
        ((s[0] = parseInt(s[0]).toLocaleString()), (r = s.join('.')));
      } else {
        r = Math.floor(e).toLocaleString();
      }
    } else {
      r =
        e < 1e21
          ? (function (e, t = 0) {
              const s = ['', 'K', 'M', 'B', 'T', 'q', 'Q', 's', 'S', 'o', 'n', 'd'],
                r = Math.floor(Math.log10(Math.abs(e)) / 3),
                i = Math.max(0, Math.min(r, s.length - 1)),
                n = s[i],
                o = e / Math.pow(10, 3 * i);
              if (0 === t && o === Math.floor(o)) {
                return o.toString() + n;
              }
              return o.toFixed(t || 1) + n;
            })(e, t)
          : e.toExponential(t);
    }
    return s ? '-' + r : r;
  }
  const u = new (class {
    constructor() {
      ((this.pendingUpdates = new Map()),
        (this.pendingStyles = new Map()),
        (this.pendingClasses = new Map()),
        (this.pendingVisibility = new Set()),
        (this.frameId = null),
        (this.enabled = !0),
        (this.elementCache = new Map()),
        (this.cacheHits = 0),
        (this.cacheMisses = 0));
    }
    getElement(e) {
      let t = this.elementCache.get(e);
      return (
        t && document.body.contains(t)
          ? this.cacheHits++
          : ((t = document.getElementById(e)),
            t && (this.elementCache.set(e, t), this.cacheMisses++)),
        t
      );
    }
    updateText(e, t) {
      if (!this.enabled) {
        const s = this.getElement(e);
        return void (s && s.textContent !== t && (s.textContent = t));
      }
      (this.pendingUpdates.set(e, () => {
        const s = this.getElement(e);
        s && s.textContent !== t && (s.textContent = t);
      }),
        this.scheduleUpdate());
    }
    updateHTML(e, t) {
      if (!this.enabled) {
        const s = this.getElement(e);
        return void (s && s.innerHTML !== t && (s.innerHTML = t));
      }
      (this.pendingUpdates.set(e, () => {
        const s = this.getElement(e);
        s && s.innerHTML !== t && (s.innerHTML = t);
      }),
        this.scheduleUpdate());
    }
    updateStyles(e, t) {
      if (!this.enabled) {
        const s = this.getElement(e);
        return void (s && Object.assign(s.style, t));
      }
      (this.pendingStyles.set(e, () => {
        const s = this.getElement(e);
        s && Object.assign(s.style, t);
      }),
        this.scheduleUpdate());
    }
    updateClasses(e, t) {
      if (!this.enabled) {
        const s = this.getElement(e);
        return void (
          s && (t.add && s.classList.add(...t.add), t.remove && s.classList.remove(...t.remove))
        );
      }
      (this.pendingClasses.set(e, () => {
        const s = this.getElement(e);
        s && (t.add && s.classList.add(...t.add), t.remove && s.classList.remove(...t.remove));
      }),
        this.scheduleUpdate());
    }
    updateVisibility(e, t) {
      if (!this.enabled) {
        const s = this.getElement(e);
        return void (s && (s.style.display = t ? '' : 'none'));
      }
      (this.pendingVisibility.add(JSON.stringify({ elementId: e, visible: t })),
        this.scheduleUpdate());
    }
    scheduleUpdate() {
      null === this.frameId &&
        (this.frameId = requestAnimationFrame(() => {
          this.flush();
        }));
    }
    flush() {
      try {
        this.frameId = null;
        new Map();
        for (const e of this.pendingVisibility) {
          const { elementId: t, visible: s } = JSON.parse(e),
            r = this.getElement(t);
          r && (r.style.display = s ? '' : 'none');
        }
        this.pendingVisibility.clear();
        for (const [e, t] of this.pendingStyles) {
          t();
        }
        this.pendingStyles.clear();
        for (const [e, t] of this.pendingClasses) {
          t();
        }
        this.pendingClasses.clear();
        for (const [e, t] of this.pendingUpdates) {
          t();
        }
        this.pendingUpdates.clear();
      } catch (e) {
        s.handleError(e, 'domBatcher.flush');
      }
    }
    setEnabled(e) {
      ((this.enabled = e), e || this.flush());
    }
    clearCache() {
      (this.elementCache.clear(), (this.cacheHits = 0), (this.cacheMisses = 0));
    }
    getCacheStats() {
      const e = this.cacheHits + this.cacheMisses;
      return {
        hits: this.cacheHits,
        misses: this.cacheMisses,
        hitRate: e > 0 ? ((this.cacheHits / e) * 100).toFixed(2) + '%' : '0%',
        size: this.elementCache.size,
      };
    }
    batch(e) {
      const t = this.enabled;
      this.enabled = !0;
      try {
        e();
      } catch (e) {
        s.handleError(e, 'domBatcher.batch');
      } finally {
        t || (this.flush(), (this.enabled = !1));
      }
    }
    read(e, t) {
      const s = this.getElement(e);
      if (!s) {
        return null;
      }
      return (
        [
          'offsetWidth',
          'offsetHeight',
          'offsetTop',
          'offsetLeft',
          'clientWidth',
          'clientHeight',
          'clientTop',
          'clientLeft',
          'scrollWidth',
          'scrollHeight',
          'scrollTop',
          'scrollLeft',
          'getBoundingClientRect',
        ].includes(t) && this.flush(),
        'getBoundingClientRect' === t ? s.getBoundingClientRect() : s[t]
      );
    }
  })();
  'undefined' !== typeof window && (window.UniversalPaperclipsDOMBatcher = u);
  const h = new (class {
    constructor() {
      ((this.elements = {}),
        (this.lastValues = {}),
        (this.initialized = !1),
        (this.updateCount = 0));
    }
    init() {
      (this.cacheElements(), (this.initialized = !0));
    }
    cacheElements() {
      ((this.elements.clips = document.getElementById('clips')),
        (this.elements.funds = document.getElementById('funds')),
        (this.elements.wire = document.getElementById('wire')),
        (this.elements.unsoldClips = document.getElementById('unsoldClips')),
        (this.elements.clipRate = document.getElementById('clipRate')),
        (this.elements.clipmakerLevel = document.getElementById('clipmakerLevel')),
        (this.elements.megaClipperLevel = document.getElementById('megaClipperLevel')),
        (this.elements.demand = document.getElementById('demand')),
        (this.elements.margin = document.getElementById('margin')),
        (this.elements.marketingLvl = document.getElementById('marketingLvl')),
        (this.elements.wireCost = document.getElementById('wireCost')),
        (this.elements.operations = document.getElementById('operations')),
        (this.elements.trust = document.getElementById('trust')),
        (this.elements.processors = document.getElementById('processors')),
        (this.elements.memory = document.getElementById('memory')),
        (this.elements.creativity = document.getElementById('creativity')),
        (this.elements.factoryLevel = document.getElementById('factoryLevel')),
        (this.elements.harvesterLevel = document.getElementById('harvesterLevel')),
        (this.elements.wireDroneLevel = document.getElementById('wireDroneLevel')),
        (this.elements.businessDisplay = document.getElementById('businessDisplay')),
        (this.elements.manufacturingDisplay = document.getElementById('manufacturingDisplay')),
        (this.elements.computationalDisplay = document.getElementById('computationalDisplay')),
        (this.elements.projectsDisplay = document.getElementById('projectsDisplay')),
        (this.elements.spaceDisplay = document.getElementById('spaceDisplay')));
    }
    render(e) {
      (this.initialized || this.init(),
        u.batch(() => {
          try {
            (this.updateResources(e),
              this.updateProduction(e),
              this.updateMarket(e),
              this.updateComputing(e),
              this.updateInfrastructure(e),
              this.updateDisplayVisibility(e));
          } catch (e) {
            s.handleError(e, 'uiRenderer.render');
          }
        }));
    }
    updateResources(e) {
      (this.updateElement('clips', e.resources.clips),
        this.updateElement('funds', e.resources.funds, !0),
        this.updateElement('wire', e.resources.wire),
        this.updateElement('unsoldClips', e.resources.unsoldClips));
    }
    updateProduction(e) {
      (this.updateElement('clipRate', e.production.clipRate),
        this.updateElement('clipmakerLevel', e.production.clipmakerLevel),
        this.updateElement('megaClipperLevel', e.production.megaClipperLevel));
    }
    updateMarket(e) {
      (this.updateElement('demand', e.market.demand, !1, 1),
        this.updateElement('margin', e.market.margin, !0),
        this.updateElement('marketingLvl', e.market.marketingLvl),
        this.updateElement('wireCost', e.market.wireCost, !0));
    }
    updateComputing(e) {
      (this.updateElement('operations', e.computing.operations),
        this.updateElement('trust', e.computing.trust),
        this.updateElement('processors', e.computing.processors),
        this.updateElement('memory', e.computing.memory),
        this.updateElement('creativity', e.computing.creativity));
    }
    updateInfrastructure(e) {
      (e.flags.factory && this.updateElement('factoryLevel', e.infrastructure.factoryLevel),
        e.flags.harvester && this.updateElement('harvesterLevel', e.infrastructure.harvesterLevel),
        e.flags.wireDrone && this.updateElement('wireDroneLevel', e.infrastructure.wireDroneLevel));
    }
    updateElement(e, t, s = !1, r = 0) {
      if (this.lastValues[e] === t) {
        return;
      }
      if (!this.elements[e]) {
        return;
      }
      let i;
      ((i = s ? '$' + p(t, r) : p(t, r)),
        u.updateText(e, i),
        (this.lastValues[e] = t),
        this.updateCount++);
    }
    updateDisplayVisibility(e) {
      (this.setDisplayVisible('businessDisplay', !0),
        this.setDisplayVisible('manufacturingDisplay', e.flags.autoClipper),
        this.setDisplayVisible('computationalDisplay', e.flags.trust),
        this.setDisplayVisible('projectsDisplay', e.flags.projects),
        this.setDisplayVisible('spaceDisplay', e.flags.space));
    }
    setDisplayVisible(e, t) {
      this.elements[e] && u.updateStyles(e, { display: t ? 'block' : 'none' });
    }
    showNotification(e, t = 3e3) {
      const s = document.createElement('div');
      ((s.className = 'notification'),
        (s.textContent = e),
        document.body.appendChild(s),
        setTimeout(() => {
          s.classList.add('visible');
        }, 10),
        setTimeout(() => {
          (s.classList.remove('visible'),
            setTimeout(() => {
              document.body.removeChild(s);
            }, 300));
        }, t));
    }
    updateButtonStates(e) {
      const t = document.getElementById('btnMakePaperclip');
      t && (t.disabled = e.resources.wire < 1);
      const s = document.getElementById('btnBuyWire');
      if (s) {
        const t = e.market.wireCost;
        s.disabled = e.resources.funds < t;
      }
      const r = document.getElementById('btnBuyAutoClipper');
      if (r) {
        const t = e.market.clipperCost;
        r.disabled = e.resources.funds < t;
      }
    }
    flashElement(e, t = '#ffff00') {
      const s = this.elements[e] || document.getElementById(e);
      if (!s) {
        return;
      }
      const r = s.style.backgroundColor;
      (u.updateStyles(e, { backgroundColor: t, transition: 'background-color 0.3s' }),
        setTimeout(() => {
          u.updateStyles(e, { backgroundColor: r });
        }, 300));
    }
  })();
  function g(e, t) {
    const s = document.getElementById(e);
    s ? s.addEventListener('click', t) : setTimeout(() => g(e, t), 100);
  }
  'undefined' !== typeof window && (window.UniversalPaperclipsRenderer = h);
  const f = new (class {
    constructor() {
      ((this.visible = !1),
        (this.container = null),
        (this.updateInterval = null),
        (this.panels = {}),
        (this.initialized = !1));
    }
    init() {
      this.initialized ||
        (this.createDashboard(),
        document.addEventListener('keydown', (e) => {
          e.ctrlKey && e.shiftKey && 'D' === e.key && (e.preventDefault(), this.toggle());
        }),
        (this.initialized = !0),
        s.info('Dev dashboard initialized (Ctrl+Shift+D to toggle)'));
    }
    createDashboard() {
      ((this.container = document.createElement('div')),
        (this.container.id = 'devDashboard'),
        (this.container.className = 'dev-dashboard'),
        (this.container.style.cssText =
          "\n      position: fixed;\n      top: 10px;\n      right: 10px;\n      width: 400px;\n      max-height: 90vh;\n      background: rgba(0, 0, 0, 0.9);\n      color: #0f0;\n      font-family: 'Courier New', monospace;\n      font-size: 12px;\n      border: 2px solid #0f0;\n      border-radius: 5px;\n      padding: 10px;\n      overflow-y: auto;\n      z-index: 10000;\n      display: none;\n    "));
      const e = document.createElement('div');
      ((e.innerHTML =
        '\n      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; border-bottom: 1px solid #0f0; padding-bottom: 5px;">\n        <h3 style="margin: 0; color: #0f0;">DEV DASHBOARD</h3>\n        <button id="devDashboardClose" style="background: none; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 2px 8px;">X</button>\n      </div>\n    '),
        this.container.appendChild(e),
        this.createPerformancePanel(),
        this.createStatePanel(),
        this.createErrorPanel(),
        this.createDOMPanel(),
        this.createControlsPanel(),
        document.body.appendChild(this.container),
        document.getElementById('devDashboardClose').addEventListener('click', () => {
          this.hide();
        }));
    }
    createPerformancePanel() {
      const e = document.createElement('div');
      ((e.className = 'dev-panel'),
        (e.style.cssText = 'margin-bottom: 10px; padding: 5px; border: 1px solid #0f0;'),
        (e.innerHTML =
          '\n      <h4 style="margin: 0 0 5px 0; color: #0f0;">PERFORMANCE</h4>\n      <div id="devPerfContent" style="font-size: 11px;"></div>\n    '),
        this.container.appendChild(e),
        (this.panels.performance = e));
    }
    createStatePanel() {
      const e = document.createElement('div');
      ((e.className = 'dev-panel'),
        (e.style.cssText = 'margin-bottom: 10px; padding: 5px; border: 1px solid #0f0;'),
        (e.innerHTML =
          '\n      <h4 style="margin: 0 0 5px 0; color: #0f0;">GAME STATE</h4>\n      <div id="devStateContent" style="font-size: 11px; max-height: 200px; overflow-y: auto;"></div>\n    '),
        this.container.appendChild(e),
        (this.panels.state = e));
    }
    createErrorPanel() {
      const e = document.createElement('div');
      ((e.className = 'dev-panel'),
        (e.style.cssText = 'margin-bottom: 10px; padding: 5px; border: 1px solid #0f0;'),
        (e.innerHTML =
          '\n      <h4 style="margin: 0 0 5px 0; color: #0f0;">ERROR LOG</h4>\n      <div id="devErrorContent" style="font-size: 11px; max-height: 150px; overflow-y: auto;"></div>\n    '),
        this.container.appendChild(e),
        (this.panels.errors = e));
    }
    createDOMPanel() {
      const e = document.createElement('div');
      ((e.className = 'dev-panel'),
        (e.style.cssText = 'margin-bottom: 10px; padding: 5px; border: 1px solid #0f0;'),
        (e.innerHTML =
          '\n      <h4 style="margin: 0 0 5px 0; color: #0f0;">DOM BATCHING</h4>\n      <div id="devDOMContent" style="font-size: 11px;"></div>\n    '),
        this.container.appendChild(e),
        (this.panels.dom = e));
    }
    createControlsPanel() {
      const e = document.createElement('div');
      ((e.className = 'dev-panel'),
        (e.style.cssText = 'margin-bottom: 10px; padding: 5px; border: 1px solid #0f0;'),
        (e.innerHTML =
          '\n      <h4 style="margin: 0 0 5px 0; color: #0f0;">CONTROLS</h4>\n      <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 5px;">\n        <button id="devAddClips" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">+10K Clips</button>\n        <button id="devAddFunds" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">+$1000</button>\n        <button id="devAddWire" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">+10K Wire</button>\n        <button id="devAddOps" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">+10K Ops</button>\n        <button id="devUnlockAll" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">Unlock All</button>\n        <button id="devResetPerf" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">Reset Perf</button>\n        <button id="devClearErrors" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">Clear Errors</button>\n        <button id="devExportState" style="background: #001; border: 1px solid #0f0; color: #0f0; cursor: pointer; padding: 5px;">Export State</button>\n      </div>\n    '),
        this.container.appendChild(e),
        (this.panels.controls = e),
        this.setupControls());
    }
    setupControls() {
      (document.getElementById('devAddClips').addEventListener('click', () => {
        (t.increment('resources.clips', 1e4), s.debug('Added 10,000 clips'));
      }),
        document.getElementById('devAddFunds').addEventListener('click', () => {
          (t.increment('resources.funds', 1e3), s.debug('Added $1,000'));
        }),
        document.getElementById('devAddWire').addEventListener('click', () => {
          (t.increment('resources.wire', 1e4), s.debug('Added 10,000 wire'));
        }),
        document.getElementById('devAddOps').addEventListener('click', () => {
          (t.increment('computing.operations', 1e4), s.debug('Added 10,000 operations'));
        }),
        document.getElementById('devUnlockAll').addEventListener('click', () => {
          (Object.keys(t.flags).forEach((e) => {
            t.set(`flags.${e}`, !0);
          }),
            s.debug('Unlocked all features'));
        }),
        document.getElementById('devResetPerf').addEventListener('click', () => {
          (r.reset(), s.debug('Performance metrics reset'));
        }),
        document.getElementById('devClearErrors').addEventListener('click', () => {
          (s.clearErrorLog(), this.updateErrorPanel());
        }),
        document.getElementById('devExportState').addEventListener('click', () => {
          const e = JSON.stringify(t, null, 2),
            r = new Blob([e], { type: 'application/json' }),
            i = URL.createObjectURL(r),
            n = document.createElement('a');
          ((n.href = i),
            (n.download = 'gamestate.json'),
            n.click(),
            URL.revokeObjectURL(i),
            s.debug('Game state exported'));
        }));
    }
    update() {
      this.visible &&
        (this.updatePerformancePanel(),
        this.updateStatePanel(),
        this.updateErrorPanel(),
        this.updateDOMPanel());
    }
    updatePerformancePanel() {
      const e = r.getMetrics();
      document.getElementById('devPerfContent').innerHTML =
        `\n      <div>FPS: <span style="color: ${e.fps < 30 ? '#f00' : '#0f0'}">${e.fps.toFixed(1)}</span> (min: ${e.minFps.toFixed(1)})</div>\n      <div>Frame Time: ${e.avgFrameTime.toFixed(2)}ms (max: ${e.maxFrameTime.toFixed(2)}ms)</div>\n      <div>Update: ${e.updateTime.toFixed(2)}ms | Render: ${e.renderTime.toFixed(2)}ms</div>\n      <div>Memory: ${e.memoryUsage.toFixed(1)}MB</div>\n    `;
    }
    updateStatePanel() {
      const e = document.getElementById('devStateContent'),
        s = t,
        r = `\n      <div style="color: #ff0;">RESOURCES</div>\n      <div>Clips: ${p(s.resources.clips)}</div>\n      <div>Funds: $${p(s.resources.funds)}</div>\n      <div>Wire: ${p(s.resources.wire)}</div>\n      <div style="margin-top: 5px; color: #ff0;">PRODUCTION</div>\n      <div>Rate: ${p(s.production.clipRate)}/sec</div>\n      <div>Auto-Clippers: ${s.production.clipmakerLevel}</div>\n      <div>Mega-Clippers: ${s.production.megaClipperLevel}</div>\n      <div style="margin-top: 5px; color: #ff0;">COMPUTING</div>\n      <div>Processors: ${s.computing.processors} | Memory: ${s.computing.memory}</div>\n      <div>Operations: ${p(s.computing.operations)}</div>\n      <div>Trust: ${s.computing.trust}/${s.computing.maxTrust}</div>\n    `;
      e.innerHTML = r;
    }
    updateErrorPanel() {
      const e = document.getElementById('devErrorContent'),
        t = s.getErrorLog(10);
      if (0 === t.length) {
        return void (e.innerHTML = '<div style="color: #666;">No errors logged</div>');
      }
      const r = t
        .reverse()
        .map((e) => {
          const t = new Date(e.timestamp).toLocaleTimeString();
          return `<div style="color: ${'error' === e.level ? '#f00' : 'warn' === e.level ? '#fa0' : '#999'}; margin-bottom: 2px;">[${t}] ${e.message}</div>`;
        })
        .join('');
      e.innerHTML = r;
    }
    updateDOMPanel() {
      const e = document.getElementById('devDOMContent'),
        t = u.getCacheStats(),
        s = window.UniversalPaperclipsRenderer;
      e.innerHTML = `\n      <div>Cache Hits: ${t.hits} | Misses: ${t.misses}</div>\n      <div>Hit Rate: ${t.hitRate} | Size: ${t.size}</div>\n      <div>Render Updates: ${s ? s.updateCount : 0}</div>\n      <div>Batching: ${u.enabled ? 'Enabled' : 'Disabled'}</div>\n    `;
    }
    show() {
      (this.initialized || this.init(),
        (this.container.style.display = 'block'),
        (this.visible = !0),
        (this.updateInterval = setInterval(() => this.update(), 100)),
        this.update(),
        s.debug('Dev dashboard shown'));
    }
    hide() {
      ((this.container.style.display = 'none'),
        (this.visible = !1),
        this.updateInterval && (clearInterval(this.updateInterval), (this.updateInterval = null)),
        s.debug('Dev dashboard hidden'));
    }
    toggle() {
      this.visible ? this.hide() : this.show();
    }
    isVisible() {
      return this.visible;
    }
  })();
  function v() {
    (s.info('Universal Paperclips - Modern Edition'),
      s.info('Original by Frank Lantz and Bennett Foddy'));
    const e = 'localhost' === window.location.hostname || '127.0.0.1' === window.location.hostname;
    (s.setLogLevel(e ? 'debug' : 'info'), e && f.init());
    try {
      t.load() ? s.info('Save game loaded successfully') : s.info('Starting new game');
    } catch (e) {
      (s.handleError(e, 'initGame.load'), s.warn('Failed to load save, starting new game'));
    }
    (i.addUpdateHandler(
      s.createErrorBoundary((e, t) => {
        const s = Date.now();
        (n.update(e),
          o.update(e, s),
          a.update(e),
          d.update(e, s),
          t.increment('ui.elapsedTime', e));
      }, 'mainUpdateHandler'),
    ),
      i.addRenderHandler(
        s.createErrorBoundary((e) => {
          (h.render(e), h.updateButtonStates(e));
        }, 'mainRenderHandler'),
      ));
    try {
      (g('btnMakePaperclip', () => {
        n.makeClip() && h.flashElement('clips', '#90EE90');
      }),
        g('btnLowerPrice', () => {
          o.adjustPrice('lower');
        }),
        g('btnRaisePrice', () => {
          o.adjustPrice('raise');
        }),
        g('btnBuyWire', () => {
          o.buyWire(1e3) && h.flashElement('wire', '#90EE90');
        }),
        g('btnMarketing', () => {
          o.buyMarketing() && h.flashElement('marketingLvl', '#90EE90');
        }),
        g('btnBuyAutoClipper', () => {
          n.buyAutoClipper() && h.flashElement('clipmakerLevel', '#90EE90');
        }),
        g('btnBuyMegaClipper', () => {
          n.buyMegaClipper() && h.flashElement('megaClipperLevel', '#90EE90');
        }),
        g('btnAddProc', () => {
          a.addProcessor() && h.flashElement('processors', '#90EE90');
        }),
        g('btnAddMem', () => {
          a.addMemory() && h.flashElement('memory', '#90EE90');
        }),
        g('btnQuantumCompute', () => {
          a.startQuantumCompute() && h.flashElement('operations', '#673AB7');
        }),
        g('btnSave', () => {
          t.save()
            ? h.showNotification('Game saved!', 2e3)
            : h.showNotification('Save failed!', 2e3);
        }),
        g('btnLoad', () => {
          t.load()
            ? (h.showNotification('Game loaded!', 2e3), location.reload())
            : h.showNotification('No save found!', 2e3);
        }),
        g('btnReset', () => {
          confirm('Are you sure you want to reset your game? This cannot be undone!') &&
            (t.reset(), location.reload());
        }),
        document.addEventListener('keydown', (e) => {
          if (
            (e.ctrlKey &&
              's' === e.key &&
              (e.preventDefault(), t.save() && h.showNotification('Game saved!', 1e3)),
            ' ' === e.key && !e.target.matches('input, textarea'))
          ) {
            e.preventDefault();
            const t = document.getElementById('btnMakePaperclip');
            t && !t.disabled && (n.makeClip(), h.flashElement('clips', '#90EE90'));
          }
        }));
    } catch (e) {
      s.handleError(e, 'initGame.setupEventHandlers');
    }
    (i.start(),
      h.render(t),
      setInterval(() => {
        try {
          t.save();
        } catch (e) {
          s.handleError(e, 'autosave.interval');
        }
      }, 3e4));
  }
  ('undefined' !== typeof window && (window.UniversalPaperclipsDevDashboard = f),
    'loading' === document.readyState ? document.addEventListener('DOMContentLoaded', v) : v(),
    (window.UniversalPaperclips = {
      errorHandler: s,
      performanceMonitor: r,
      devDashboard: f,
      gameState: t,
      gameLoop: i,
      productionSystem: n,
      marketSystem: o,
      computingSystem: a,
      combatSystem: d,
      projectsSystem: m,
      uiRenderer: h,
      debug: {
        getState: () => t,
        setState: (e, s) => t.set(e, s),
        addClips: (e) => t.increment('resources.clips', e),
        addFunds: (e) => t.increment('resources.funds', e),
        addWire: (e) => t.increment('resources.wire', e),
        unlockAll: () => {
          Object.keys(t.flags).forEach((e) => {
            t.set(`flags.${e}`, !0);
          });
        },
        reset: () => {
          confirm('Are you sure you want to reset the game?') && (t.reset(), location.reload());
        },
        getErrors: () => s.getErrorLog(),
        clearErrors: () => s.clearErrorLog(),
        getPerformance: () => r.getReport(),
        resetPerformance: () => r.reset(),
        setLogLevel: (e) => s.setLogLevel(e),
      },
    }),
    s.info('Game loaded. Use window.UniversalPaperclips.debug for debugging tools.'));
})();
